query GetUserGroupsById($userId: Int, $offset: Int, $limit: Int, $accepted: Boolean) {
	group_user(
		where: { userId: { _eq: $userId }, accepted: { _eq: $accepted } }
		offset: $offset
		limit: $limit
		order_by: [{ group: { updatedAt: desc } }, { id: desc }]
	) {
		...UserGroupFields
	}
}
