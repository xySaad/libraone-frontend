query GetUserGroupsById($userId: Int, $offset: Int, $limit: Int) {
	group_user(
		where: { userId: { _eq: $userId } }
		offset: $offset
		limit: $limit
		order_by: { id: desc }
	) {
		...UserGroupFields
	}
}
